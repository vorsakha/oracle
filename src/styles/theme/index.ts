import colors from './colors';
import fonts from './fonts';
import sizes from './sizes';

export enum Theme {
  light = 'light',
  dark = 'dark',
}

export const theme = {
  colors,
  fonts,
  sizes,
};
